<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CleanList" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/backgroundDark</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="actionBarStyle">@style/ActionBarText</item>
    </style>

    <style name="CardViewTheme" parent="CardView.Dark">
        <item name="cardBackgroundColor">@color/black</item>
        <item name="android:background">@color/black</item>
    </style>

    <style name="CardViewItem" parent="Theme.MaterialComponents">
        <item name="android:background">@color/black</item>
    </style>

    <style name="Backgrounds" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:background">@color/backgroundDark</item>
    </style>

    <style name="ActionBarText" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>